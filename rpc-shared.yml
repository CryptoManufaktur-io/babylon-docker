# To be used with babylon.yml
services:
  babylon:
    ports:
      - ${CL_RPC_PORT:-26657}:${CL_RPC_PORT:-26657}/tcp
      - ${CL_REST_PORT:-1317}:${CL_REST_PORT:-1317}/tcp
      - ${RPC_PORT:-8545}:${RPC_PORT:-8545}/tcp
      - ${WS_PORT:-8546}:${WS_PORT:-8546}/tcp
